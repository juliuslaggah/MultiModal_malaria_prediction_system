services:
  - type: web
    name: malaria-frontend
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://malaria-backend.onrender.com  # Will update after backend deploys

